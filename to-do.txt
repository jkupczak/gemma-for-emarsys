# URGENT BUGS

None


-----------------------------------------------
# HOT

Deprecate recently used tab and roll the functionality into recently seen. Recently seen images in storage should get a property for the used date. Images that were recently used should get some sort of visual highlight so that they stand out among the seen images that have not been used. Recently used images should not get pushed out as fast as unused recently seen image? Maybe they are part of a separate list that is capped at 100. Add a category grouping for Recently used by Day.

Grouping by "Last Seen" presents the images with the oldest at the top. Reverse this.

Sometimes i cant open the edit snippet modal

Add disabled placeholders for the language and version selectboxes so that the UI is consistent when the real ones are unavailable. Clicking on these fake ones should trigger a click on the appropriate tab so they can edit them.

  ```
  <cb-version-selector class="cb-header-button e-layout__action">
    <e-select inline="">
      <select class="e-select e-select-inline">
        <option value="3503412" selected="true">Version #a</option>
        <option value="3503413">Version #b</option>
        <!---->
      </select>
      <div class="e-selectnew__wrapper">
        <!--<>-->
        <div role="button" aria-haspopup="listbox" class="e-selectnew e-selectnew-native e-selectnew-inline" tabindex="0" aria-label="Version #a" aria-expanded="false" aria-disabled="false"></div>
        <!--</>-->
      </div>
    </e-select>
  </cb-version-selector>
  ```

Add Expanded Mode as a toggle in the settings panel

In expanded mode, the hidden nav on the left should only open on click.

#Rich Paste: After rich pasting, MCPASTEBIN seems to be getting added and takes up space until the user blurs the block. Sometimes pasting into buttons creates a SECODN button in the mobile preview which suggests something else is getting pasted.

#Rich Paste: Sometimes Rich Paste inserts the paste inside of the existing text even though we did CMD+A to select all of the existing text.

gemRecentlySeenImageGroupCollapse in localstorage is holding onto the collapse status of Recently Seen groupings that are no longer visible to the user. Once a group is no longer available in Recently seen, the matching key in gemRecentlySeenImageGroupCollapse should be removed so that the log size does not get out of control.

Move favorited images from local storage to sync storage

Measure total space taken up by snippets in sync storage. Calculate how much space is left. Create a storage space UI indicator for the user.

Search pills should take two clicks to close.

Search pills should be draggable to rearrange

Hide the "Title" input in the Links dialog by default. Provide a toggle to turn it back but explain why it was hidden by default.

Keep track of session IDs and which one is most recent. Detect if we have any pages open with a different session ID and throw up a notice that saving could be affected and recomend a page refresh before making further edits.

Can I create "drag for padding" functionality? Would need to ask the user to designate an integere based variable for top and bottom padding in the settings panel.

When the Block Style Settings panel is empty, it should close on its own.

In expanded mode, the block style settings should not cover the top row where we keep the email name and save button

The block reset button in the toolbar should have a confirmation prompt.

add text highlighting to the subject line, preview text, and conditional text editors

Add a red notification dot on the links panel for bad or missing links, or untracked links
 - '_blank' should be considered an invalid URL

Add a gear icon to the content block toolbar to open the settings panel and scroll to a new section
where they can toggle icons on or off

Use the Emarsys icon font

Can I add a note to the content block about what block targeting is being done?

-----------------------------------------------
# NO 

✗ Can I open the block /variable settings panel just because the user clicked on the block? - REMOVED: block-level-variables-opener.js removed

Allow users to resize the Emarsys nav panel. Keep track of the size they set each panel to and 
use that size whenever they return to that panel

-----------------------------------------------
# COMPLETE

✓ The mobile image picker view retains the desktop image preview even if the mobile image is not set. This corrects itself after the user clicks away from the focus that is automatically applied to the URL field. Lets fix that.

✓ Search terms should get added as active pills when you hit enter/tab? Moved to a new line undernear the search bar. Clicking them deactivates them but keeps them there, clicking an X on them removes them entirely. Search results should take all active pills and the search box into account. For example, right now searching for 'e4 uk' would not return images with the filename uk_e4.png. But searching with pills should treat each search term separately. Results should include ALL search terms, just not necesarrily in any specific order.

✓ Add an "Export" button in the Snippet edit window so that users can share just one snippet at a time. Add an export option that introduces checkboxes to the full list so that the user can select multiple instead of exporting them all. Add a "select all/deselectall" button in this mode.

✓ When a search pill is deactivated, if the value in the search box is identical, clear the search box too.

✓ Move the "group by" folder to the right, search box to the left

✓ When users hover over images, show the ALT text. Make this a button in the toolbar that is on by default and its on/off status persists across pages. Font-size of the alt text should scale based on image size between 12px and 16px? Offset it above the image with a dark background. It should not be clickable, use pointer-events: none. Consider three toggles: Off, On with Hover, Always On.

✓ Adjust the image thumbnail height in the image picker to be taller the fewer columns there are. Also allow two columns as the minimum.

✓ Update expanded mode CSS to eliminate the spacing around the left column on the top, left, and bottom. Remove all borders except for the right border. Remove the border-radius. Add padding to the left so that the tab nav looks the same and not squished.

✓ Alternative text input needs a snippet swap icon

✓ Search pills and input needs a regex option.

✓ The image picker popups that show metadata should show a bigger preview of the image.

✓ "Recently Seen" logging function doesn't work when the Media DB page is being viewed in thumbnail mode.

✓ Add a toggle in settings for the email preview header. Let them change it between vertical and horizontal.

✓ Add a shortcut to quickly cycle through languages, like CMD+SHIFT+arrow keys. When a new one is loaded, show a toast that says what language it is in case the language selector isn't currently visible.

✓ While the ESL syntax checker is thinking, you should not be able to click OK to click the dialog

✓ Rich Paste should trim white space

✓ Double click to open esl tokens and ✓ image properties

## Image Picker:
    ✓ Let them apply a custom name to it (like "Ends Tomorrow Talker")
    ✓ Perhaps add "Recent" and Favorites" buttons in the Image Propertiers dialog next to the "Media DB" button. Clicking on either reveals
      a scrollable list of those files. Populate the image URL field by clicking + on them.
    ✓ The list should take the place of the image preview on the left.
    ✓ It should default to a table list of text (with small thumbnails)
    ✓ It should have a toggle to show images in a grid as larger thumbnails.
    ✓ Add an option to pin/favorite specific images files in the Media DB. Fine alternative to favoriting folders.
    ✓ Favorites should be able to be categorized. Categories should be collapsable.
    ✓ Add a recently "seen" tab. Collect image urls whenever we use MediaDB.
    ✓ Let users apply metadata to favorited images: search keyword, category, language

✓ The Recently Seen list should be categorized by Path. Categories should be sorted by most recently seen.

✓ When adding a Favorite Image, the Enter key closes the Image Props dialog instead of pressing OK on the favorite image modal.

✓ Search results from the media picker are being saved to Recently Seen in whatever the last seen folder is. Update it so that they get added to a "Searched" folder instead since we can't determine their actual folder.

✓ Add text replacement to the preheader box.

✓ Can I create a way to quickly replace a string of text in a block with a snippet that has the exact same name?
✓ Add Snippets tab to vertical navigation - DONE: Created snippets-tab.js
✓ Auto-focus to the input field when opening the link modal
✓ Whnen the Block Style Settings panel is open, ESC should close it. - DONE: Created overlay-panel-controls.js
✓ Make the Gemma purple theme global for all pages. On by default. Can be switched back to classic blue in settings.
✓ Add image thumbnails to the editor, and also to the media selection popup
✓ Add zebra stiping to the media selection popup
✓ Sync for the mobile preview is not updating when a mobile image is added
✓ in the media popup, move the + to the left side of the table row, increase click area

## Media Directory
✓ Add column toggles for the media picker.
x Add a recently visited dropdown for folders. Values determined by watching for the user to pick an image with the + icon and then logged the folder location.
x Add a pinned folder dropdown to the media picker.

## Snippets
✓ Snippet panel needs a scrollbar
✓ Snippets need a Category field
✓ Snippets need a description field
✓ Snippets should be visually listed by category as different tables.
✓ Category tables should be collapsible. Collapsed tables should remain collapsed on subsequent page loads.
✓ Categories and snippets should be displayed alphabetically.
✓ Categories and snippets should be pinnable so that they stay at the top.

✓ When we modify a block with the block styles panel, sometimes the id (id="mce_8") of the contenteditable element changes (id="mce_9"). This throws off Gemma and the content block toolbar no longer loads in our custom buttons. Doesn't get fixed until the page refreshes.